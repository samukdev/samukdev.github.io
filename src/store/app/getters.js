export function getDrawerState(state) {
  return state.isDrawerOpen;
}

export function getAnchors(state) {
  return state.anchors;
}

export function getSocialLinks(state) {
  return state.socialLinks;
}
